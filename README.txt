1 - No special build instructions required.
2 - ### THIRD PARTY CONTENT ###

  compile 'com.google.code.gson:gson:2.8.2'
  
  Gson is a Java library that can be used to convert Java Objects into their JSON representation. It can also be used to convert a JSON string to an equivalent Java object. Gson can work with arbitrary Java objects including pre-existing objects that you do not have source-code of. Used to share movie's object data from main to secondary activity.
  
  compile 'com.uncopt:android.justified:1.0'
  
  Justified TextField used to improve movie's description readability.
  